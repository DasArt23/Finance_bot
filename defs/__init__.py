from defs.tests import *
from defs.commands import *
from defs.but_reacts import *
commands = [
    (test1, "test1"), (test2, "test2"), (start, "start"), (help_inf, "help"),
]
but_reacs = [
    (month_report, "отчёт за время"), 
    (automatic_report, "поставить автоматический отчёт"),
    (add_expenses, "добавить затраты"),
    (add_income, "добавить доходы"),
]
